# Direct Lane examples

`bdb-local-envelope-v1.json` is a synthetic transport example only. Replace the session identifier, repository alias, exact local base SHA, timestamps, operation, revision, and payload before submitting it.

Do not place credentials, absolute repository paths, arbitrary process commands, or unrestricted profile identifiers in an envelope.
